@model List<ProCareMvc.Models.Order>
@{
    ViewData["Title"] = "My Orders";
}
<h1>My Orders</h1>
<p>Here are your orders:</p>
<table class="table">
    <thead>
        <tr>
            <th>Order ID</th>
            <th>Order Date</th>
            <th>Total Amount</th>
            <th>Status</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.OrderId</td>
                <td>@order.OrderDate.ToString("MM/dd/yyyy")</td>
                <td>@order.TotalAmount.ToString("C")</td>
                <td>@order.Status</td>
                <td><a class=" btn btn-success" asp-controller="Home" asp-action="OrderDetails" asp-route-id="@order.OrderId">Details</a></td>
            </tr>
        }
    </tbody>
